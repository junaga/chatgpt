{
  "name": "chatgpt-linux",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "port": "tsx src/cli.ts",
    "test": "npm run typecheck && npm run test:tool",
    "test:tool": "tsx --test test/*.test.ts",
    "test:installed": "npm test --prefix desktop",
    "test:live": "npm run test:live --prefix desktop",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@electron/asar": "4.2.1"
  },
  "devDependencies": {
    "@types/node": "24.10.0",
    "tsx": "4.23.1",
    "typescript": "5.9.3"
  }
}
